const ButtonSortByName = ({ sortByName }) => {

    return (
        <button onClick={sortByName} className="btn-sort">
            Sort By Name
        </button>
    );
}

export default ButtonSortByName;