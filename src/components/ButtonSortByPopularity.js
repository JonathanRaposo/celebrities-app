
const ButtonSortByPopularity = ({ sortByPopularity }) => {

    return (
        <button onClick={sortByPopularity} className="btn-sort">
            Sort By Popularity
        </button>
    )
}

export default ButtonSortByPopularity;