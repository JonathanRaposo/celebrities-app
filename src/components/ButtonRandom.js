const ButtonRandom = ({ AddRandomContact }) => {

    return (
        <button onClick={AddRandomContact} className="btn-random">
            Add Random contact
        </button>
    )

}

export default ButtonRandom;